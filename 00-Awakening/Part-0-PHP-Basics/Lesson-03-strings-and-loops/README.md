# Lesson 03 — Strings and Loops

Status: Not cleared

## Learn
- Strings are sequences of characters
- `for`, `while`, `foreach` — prefer `foreach` for arrays

## CHECK
Write a loop that counts characters in a string without looking at notes.

## Practice
Count how many times `a` appears in a sample string.
