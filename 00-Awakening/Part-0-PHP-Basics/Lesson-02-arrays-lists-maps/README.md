# Lesson 02 — Arrays: Lists vs Maps

Status: Not cleared

## Learn
- PHP arrays are the workhorse structure
- List: `[10, 20, 30]` (integer keys)
- Map: `['id' => 1, 'name' => 'Hunter']` (string keys = hash map feel)

## Backend Link
APIs and DB rows often become associative arrays in PHP.

## CHECK
When would you use a list vs a map?

## Practice
Build one list and one map; print each with `foreach`.
