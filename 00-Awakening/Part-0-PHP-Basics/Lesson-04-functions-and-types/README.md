# Lesson 04 — Functions and Types

Status: Not cleared

## Learn
Prefer typed signatures:

```php
function add(int $a, int $b): int
{
    return $a + $b;
}
```

## CHECK
Why do typed params help in interview and backend code?

## Practice
Write `maxOfTwo(int $a, int $b): int` and call it.
